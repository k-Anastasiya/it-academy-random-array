import java.util.Scanner;

public class Ranner {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.println("Введите размер массива" );
        int size = scanner.nextInt();
        Array array = new Array(size);
      //  System.out.println(Arrays.toString(array.getRandom(int []array)));


    //    int numberPositve = 0;
   //     int[] array = new int[100];
  //      for (int i = 0; i < array.length; i++) {

    //        array[i] = (int) Math.round((Math.random() * (200 + 1) - 100));
     //   }


      //  for (int number : array) {
     //       if (number % 2 != 0 && number > 0) {
      //          numberPositve++;

    //        }
  //      }
        int result;
    //    System.out.println(Arrays.toString(array.getRandomArray(result)));
 //       System.out.println("Kоличество положительных нечетных чисел в этом массиве " + numberPositve);

        int RandomArray = 0;
   //     System.out.println(R);


    }
}